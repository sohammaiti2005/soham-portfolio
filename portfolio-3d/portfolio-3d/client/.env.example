# Leave this file unused for local development — the Vite proxy handles it.
# For a deployed/production build, copy this to ".env.production" and set
# VITE_API_URL to your live backend's URL (e.g. from Render).

VITE_API_URL=https://your-backend-name.onrender.com
